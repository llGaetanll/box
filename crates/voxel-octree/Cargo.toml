[package]
name = "voxel-octree"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]
gpu-prim = { path = "../gpu-prim" }

[lints]
workspace = true

[dev-dependencies]
voxel-tree64 = { path = "../voxel-tree64" }
bytemuck = { workspace = true }
